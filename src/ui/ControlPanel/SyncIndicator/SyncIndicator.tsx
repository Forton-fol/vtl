import React, { useEffect, useState } from "react";
import { useTranslation } from "react-i18next";
import { SyncStatus, onSyncStatus } from "../../../api/autoSync";
import { getToken } from "../../../api/auth";

export function SyncIndicator(): null {
  return null;
}
